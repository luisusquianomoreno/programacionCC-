#include <stdio.h>
int main (void) {
    int numero1, numero2;
    printf ("Tabla de multiplicar sencilla");
    printf ("\nIntroduce un digito: ");
    scanf ("%1d", &numero1);
    printf ("\nEl numero que has seleccionado es: %d", numero1);

    return 0;
}